# Memcached

# Up
```bash
$ sudo docker compose up -d
```

# Test
```bash
(env) $ python sample-memcached.py
```

# Deactivate Virtual Environment
```bash
(env) $ deactivate
```

# Down
```bash
$ sudo docker compose down
```
