services:
  questdb:
    image: questdb/questdb:8.2.1
    container_name: questdb
    restart: always
    ports:
      - 9000:9000
      - 8812:8812
      - 9009:9009
    environment:
      TZ: "Asia/Tokyo"
    volumes:
      - questdb-data:/var/lib/questdb

volumes:
  questdb-data:
