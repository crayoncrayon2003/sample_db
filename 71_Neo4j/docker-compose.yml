version: "3.8"

services:
  neo4j:
    image: neo4j:5.26.0
    container_name: neo4j
    restart: unless-stopped
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: neo4j/password
      TZ: "Asia/Tokyo"
    volumes:
      - neo4j-data:/data

volumes:
  neo4j-data:
